import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./styles.css"; // Optional: Create this file for CSS

ReactDOM.render(<App />, document.getElementById("root"));